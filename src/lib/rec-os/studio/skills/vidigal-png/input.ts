/**
 * Sprint REC OS Studio Foundation V0.1 — input da skill Vidigal PNG.
 * A skill não precisa de um formato mais estreito que o briefing
 * genérico do Studio hoje -- alias explícito em vez de duplicar campos.
 */
import type { StudioBriefInput } from "../../types";

export type VidigalPngInput = StudioBriefInput;
