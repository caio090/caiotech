/**
 * Sprint REC OS Studio Foundation V0.1 — contrato de saída da skill
 * Vidigal PNG. As 8 etapas oficiais do prompt mestre
 * (docs/product-roadmap/vidigal-png-master-prompt.txt, seção 12).
 * Contrato apenas -- nenhum conteúdo real é gerado nesta sprint.
 */

export interface VidigalPngOutputContract {
  briefReading: string;
  creativeDirection: string;
  conceptualBasis: string;
  visualStructure: string;
  visualGuidelines: string;
  generationPrompt: string;
  variations: string[];
  adaptations: string[];
}
