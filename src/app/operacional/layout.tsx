import { OperacionalLayoutShell } from "./_layout-client";

export default function OperacionalLayout({ children }: { children: React.ReactNode }) {
  return <OperacionalLayoutShell>{children}</OperacionalLayoutShell>;
}
