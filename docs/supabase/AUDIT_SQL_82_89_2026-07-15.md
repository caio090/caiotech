# Auditoria SQL 82-89 - 2026-07-15

Status: somente leitura / sem execucao de DDL

## Escopo

Esta auditoria consolida o estado conhecido dos SQLs 82 a 89 e do corretivo 85.

Regra aplicada nesta rodada:

- nenhum SQL foi executado no Supabase;
- nenhum dado foi alterado;
- nenhuma tabela, policy, trigger, funcao ou indice foi criado/removido;
- a auditoria live do catalogo ficou bloqueada por ausencia de canal autenticado de leitura.

## Evidencia de bloqueio da auditoria live

No workspace local foram encontrados apenas:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

Nao havia:

- `SUPABASE_SERVICE_ROLE_KEY` local;
- `DATABASE_URL`/connection string de leitura;
- Supabase CLI disponivel no PATH;
- MCP/connector de SQL com ferramenta de execucao catalogada nesta sessao.

Conclusao: o estado real no banco para as tabelas abaixo permanece `unknown` nesta auditoria. Qualquer reconciliacao deve partir de uma leitura segura no Supabase antes de executar DDL.

## Resumo por SQL

| SQL | Estado historico informado | Estado live nesta auditoria | Observacao |
|---|---|---|---|
| 82 | attempted_failed_partial_unknown | unknown | Define especialidades, comentarios de tarefa e `assigned_profile_id`. Pode ter aplicado parcialmente. |
| 84 | attempted_failed_partial_unknown | unknown | Define `work_sessions`, forecast e campos de effort/deadline em tasks. Pode ter aplicado parcialmente. |
| 85 | not_executed | unknown | Corretivo local; nao executar sem leitura do catalogo. |
| 86 | attempted_failed_partial_unknown | unknown | Integracoes genericas e links de provider. |
| 87 | attempted_failed_partial_unknown | unknown | Foundation de design; contem inconsistencias de constraint. |
| 88 | attempted_failed_partial_unknown | unknown | Links de conversas externas. |
| 89 | attempted_failed_partial_unknown | unknown | Publicacoes agendadas e canais sociais. |
| 90 | **failed** | failed | Tentado/executado e falhou. Arquivo: `90-reconcile-partial-foundations.sql`. Nao re-executar. |

## Objetos esperados por SQL local

### SQL 82

| Objeto | Tipo | Esperado pelo arquivo local | Risco/observacao |
|---|---|---|---|
| `professional_specialties` | tabela | `id`, `slug`, `name`, `description`, `created_at`; seed de especialidades | Live desconhecido. |
| `profile_specialties` | tabela | `profile_id`, `specialty_id`, `created_at`, PK composta | Policies de leitura/escrita por usuario. |
| `operational_task_comments` | tabela | `task_id`, `author_id`, `body`, `is_internal`, timestamps | `author_id uuid NOT NULL REFERENCES profiles(id) ON DELETE SET NULL` e inconsistente. |
| `operational_tasks.assigned_profile_id` | coluna | FK para `profiles(id)` | Aplicacao parcial possivel. |
| `otc_task_id_idx` | indice | indice por `task_id` | Live desconhecido. |
| `otc_*` | policies | `team_read`, `client_read`, `author_insert`, `author_update` | Pode depender de `is_internal`; confirmar antes de recriar. |

### SQL 84

| Objeto | Tipo | Esperado pelo arquivo local | Risco/observacao |
|---|---|---|---|
| `work_sessions` | tabela | `profile_id`, `client_id`, `task_id`, `area`, `source`, `started_at`, `ended_at`, `duration_min` generated | Se criada parcialmente sem `profile_id`, SQL 85 tenta corrigir. |
| `ws_profile_idx` | indice | por `profile_id` | Live desconhecido. |
| `ws_task_idx` | indice | por `task_id` | Live desconhecido. |
| `ws_client_idx` | indice | por `client_id` | Live desconhecido. |
| `operational_tasks.estimated_hours` | coluna | numeric | Live desconhecido. |
| `operational_tasks.actual_hours` | coluna | numeric | Live desconhecido. |
| `operational_tasks.deadline` | coluna | timestamptz | Live desconhecido. |
| `v_area_effort_forecast` | view | consolidacao por cliente/area | Confirmar dependencia de colunas antes de recriar. |
| `ws_*` | policies | `own_read`, `own_write`, `own_update` | Dependem de `profile_id`. |

### SQL 85

Arquivo corretivo planejado, nao executado. Ele tenta:

- adicionar `operational_task_comments.is_internal`;
- recriar policies `otc_*`;
- adicionar `work_sessions.profile_id`;
- recriar policies `ws_*`.

Risco: nao deve ser usado cegamente porque SQL 82/84 podem estar parcialmente aplicados de formas diferentes.

### SQL 86

| Objeto | Tipo | Esperado pelo arquivo local | Risco/observacao |
|---|---|---|---|
| `integration_connections` | tabela | conexoes por cliente/provider/status/config/secret_ref | Precisa confirmar se secrets nunca sao expostos em UI/API. |
| `integration_webhook_events` | tabela | eventos recebidos por provider/idempotency/status | Confirmar indice unico antes de ingerir eventos. |
| `provider_user_links` | tabela | vinculo `profile_id` + provider | Unique `(profile_id, provider)` esperado. |
| policies admin/user | policies | admin gerencia, usuario le links proprios | Live desconhecido. |

### SQL 87

| Objeto | Tipo | Esperado pelo arquivo local | Risco/observacao |
|---|---|---|---|
| `design_projects` | tabela | projeto de design por cliente/provider/status | `created_by NOT NULL ON DELETE SET NULL` inconsistente. |
| `design_versions` | tabela | versoes por projeto, `provider_data jsonb` | `created_by NOT NULL ON DELETE SET NULL` inconsistente; falta unique `(design_project_id, version)`. |
| `design_templates` | tabela | templates globais/client-specific | Sem governanca de storage/preview. |
| policies admin | policies | admin gerencia tudo | Falta regra operacional/cliente se necessario. |

Decisao pendente para SQL 87:

- Opcao A: manter `ON DELETE SET NULL` e tornar `created_by` nullable.
- Opcao B: manter `created_by NOT NULL` e trocar FK para `ON DELETE RESTRICT` ou `NO ACTION`.

Recomendacao pragmatica: usar Opcao A para preservar historico quando um profile for removido, desde que o produto aceite autoria nula em registros antigos.

### SQL 88

| Objeto | Tipo | Esperado pelo arquivo local | Risco/observacao |
|---|---|---|---|
| `conversation_links` | tabela | conversa externa por provider, client e responsavel | Unique `(provider, external_conversation_id)`. |
| policies | policies | admin gerencia, assigned user le proprio | Confirmar se cliente final deve ler algo. |

### SQL 89

| Objeto | Tipo | Esperado pelo arquivo local | Risco/observacao |
|---|---|---|---|
| `scheduled_publications` | tabela | publicacao agendada por cliente/provider/channel/status | `created_by NOT NULL ON DELETE SET NULL` inconsistente. |
| `publication_attempts` | tabela | tentativas por publicacao | OK conceitual, live desconhecido. |
| `social_channel_links` | tabela | canais sociais por cliente/provider/channel | Unique `(client_id, provider, channel)` esperado. |
| policies | policies | admin gerencia/visualiza | Falta modelo cliente/operacional se necessario. |

## Query de auditoria recomendada para leitura manual

Executar manualmente no Supabase SQL Editor somente como leitura:

```sql
select schemaname, tablename
from pg_tables
where schemaname = 'public'
  and tablename in (
    'professional_specialties',
    'profile_specialties',
    'operational_task_comments',
    'work_sessions',
    'integration_connections',
    'integration_webhook_events',
    'provider_user_links',
    'design_projects',
    'design_versions',
    'design_templates',
    'conversation_links',
    'scheduled_publications',
    'publication_attempts',
    'social_channel_links'
  )
order by tablename;

select table_name, column_name, data_type, is_nullable, column_default
from information_schema.columns
where table_schema = 'public'
  and table_name in (
    'professional_specialties',
    'profile_specialties',
    'operational_task_comments',
    'work_sessions',
    'integration_connections',
    'integration_webhook_events',
    'provider_user_links',
    'design_projects',
    'design_versions',
    'design_templates',
    'conversation_links',
    'scheduled_publications',
    'publication_attempts',
    'social_channel_links'
  )
order by table_name, ordinal_position;

select schemaname, tablename, policyname, cmd, qual, with_check
from pg_policies
where schemaname = 'public'
  and tablename in (
    'profile_specialties',
    'operational_task_comments',
    'work_sessions',
    'integration_connections',
    'integration_webhook_events',
    'provider_user_links',
    'design_projects',
    'design_versions',
    'design_templates',
    'conversation_links',
    'scheduled_publications',
    'publication_attempts',
    'social_channel_links'
  )
order by tablename, policyname;
```

## Conclusao

Nao ha base segura para declarar SQL 82, 84, 86, 87, 88 ou 89 como completo. O estado correto para todos nesta entrega e `partial_unknown`/`unknown` ate uma leitura live do catalogo.

O proximo passo tecnico e revisar manualmente esta auditoria, rodar apenas as queries SELECT acima no Supabase e entao decidir se o arquivo `docs/supabase/90-reconcile-partial-foundations.sql` pode ser adaptado/executado.
